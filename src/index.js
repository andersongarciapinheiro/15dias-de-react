import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

                //ReactDOM.render(oque esta sendo renderizado, onde esta sendo renderizado)
ReactDOM.render(<App />, document.getElementById('root'));
